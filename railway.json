{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd web && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd web && npm start",
    "restartPolicyMaxRetries": 5
  }
}